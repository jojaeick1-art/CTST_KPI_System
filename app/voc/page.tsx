import type { Metadata } from "next";
import { VocPlaceholderContent } from "./voc-placeholder";

export const metadata: Metadata = {
  title: "KPI VOC",
  description: "CTST KPI — KPI VOC",
};

export default function VocPage() {
  return <VocPlaceholderContent />;
}
